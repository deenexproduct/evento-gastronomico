# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Local env files
# Se ignora todo .env salvo el ejemplo: un `git add -A` no puede llevarse
# un endpoint o una clave por descuido.
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Reference repo (no incluir en el deploy)
_deenex-source/

# Claude Code local dev config (paths absolutos del dev)
.claude/

# Testing
test-results/
playwright-report/

# Artefactos de medición y capturas de auditoría: no son del proyecto.
.medicion/

# Revisión de la tarjeta social: variante oscura y miniaturas para mirar
# antes de publicar. No son del proyecto.
herramientas/_revision/
